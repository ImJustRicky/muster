# Muster hooks for {{SERVICE_NAME}}
# Generated by muster setup — customize these recipes
# Docs: https://just.systems

set shell := ["bash", "-euo", "pipefail", "-c"]
set dotenv-load

service := "{{SERVICE_NAME}}"
port := "{{PORT}}"

# Deploy the service
deploy:
    @echo "Deploying {{SERVICE_NAME}}..."
    # TODO: Add your deploy commands
    @echo "{{SERVICE_NAME}} deployed"

# Check service health
health:
    @echo "Checking {{SERVICE_NAME}} health..."
    # TODO: Add health check (exit 0 = healthy, exit 1 = unhealthy)
    @exit 0

# Rollback to previous version
rollback:
    @echo "Rolling back {{SERVICE_NAME}}..."
    # TODO: Add rollback commands

# Stream service logs
logs:
    @echo "Streaming {{SERVICE_NAME}} logs..."
    # TODO: Add log streaming

# Clean up resources
cleanup:
    @echo "Cleaning up {{SERVICE_NAME}}..."
    # TODO: Add cleanup commands
